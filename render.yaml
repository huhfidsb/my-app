services:
  - type: web
    name: my-app
    env: node
    plan: free
    buildCommand: npm install --include=dev
    startCommand: npm start
    envVars:
      - key: PORT
        value: 8888
      - key: DATABASE_URL
        sync: false
      - key: SESSION_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
      - key: SMTP_HOST
        sync: false
      - key: SMTP_PORT
        value: 587
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASS
        sync: false
      - key: MAIL_FROM
        sync: false
